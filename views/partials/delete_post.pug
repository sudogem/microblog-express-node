h2 Delete Post
hr
div(class='blog-post')
  h2(class='blog-post-title') {{post.title}}
  p(class='blog-post-meta') {{post.created_at}}
  div {{post.text}}

  div(class='actionButton')
    p
      | Are you sure you want to delete this post?
      button(class='btn btn-danger btn-round' ng-click='deletePost()') Yes
      |
      button(class='btn btn-info btn-round' ng-click='home()') No thanks
